# История изменений — apartx-ui

## 2026-07-18

### Версия 0.5.3

### fix: `Popover` учитывает overlay-layer — меню больше не уходит под layered Dialog

* Контент `Popover` портируется в `<body>` на фиксированном `z-50`, поэтому внутри
  оверлея с layer-контекстом (modal-registry `<Dialog>`, любой kit Dialog — он
  публикует свой z через `provideOverlayZ`) меню оказывалось ПОД телом диалога:
  элементы видимы, но клики перехватывал диалог (репро: контекст-меню контакта в
  адресной книге кабинета — e2e `contacts-modal` edit/remove). Теперь `Popover`
  читает `getOverlayLayer()` и рендерит контент на `layer.z + 2`, модальный скрим
  на `layer.z + 1` — зеркально `Select`/`Combobox`. Без layer-контекста поведение
  прежнее (`z-40`/`z-50`).

### Версия 0.5.2

### fix: `useSvelteKitNavigation()` мемоизирует адаптер — чинит history-back для оверлеев

* **`useSvelteKitNavigation()` теперь мемоизирует SK-history-адаптер** (module-singleton `skAdapter`
  через `??=`) вместо создания нового на каждый вызов. Svelte пере-прогоняет setup-эффект layout'а
  (в демо — несколько раз), а хост мог звать хук в нескольких местах → создавалось несколько
  адаптеров, и провод оверлеев расщеплялся: back-interceptor садился на ПЕРВЫЙ адаптер (one-shot
  guard `inited` в `initOverlayStack`), а `setHistoryAdapter` переуказывал ленивый push/goBack на
  ПОЗДНИЙ. Итог: браузерный BACK снимал синтетическую history-запись, но НЕ закрывал оверлей (и
  каждый вызов протекал ещё одним popstate-листенером). Мемоизация схлопывает в один адаптер / один
  popstate-листенер. Проявлялось в SvelteKit-демо (v0.5.1 включил `setHistoryAdapter`); e2e-регрессия
  `closes via browser BACK` в `e2e/overlays.spec.ts`.

## 2026-07-18

### Версия 0.5.1

### SvelteKit-хост: `useSvelteKitNavigation()` регистрирует адаптер глобально + e2e-харнесс демо

* **`useSvelteKitNavigation()` теперь регистрирует SvelteKit-адаптер как активный backend
  (`setHistoryAdapter(adapter)`) и возвращает единый дефолтный overlay-stack**, а не строит
  локальный. Компоненты кита закрываются над module-singleton `defaultOverlayStack`
  (`useOverlay` → `registerOverlay`), чей `lazyAdapter` читает `getHistory()` per-call. Раньше хук
  создавал приватный стек и НЕ звал `setHistoryAdapter`, поэтому под SvelteKit компоненты сидели на
  browser-адаптере и **Dialog не закрывался по ✕/Escape/scrim** (баг проявлялся только в демо-
  плейграунде — у Meteor-консюмеров свой адаптер уже зарегистрирован). `overlay-stack.ts` экспортит
  `defaultOverlayStack`, чтобы хост вернул именно синглтон-стек. Демо-`+layout.svelte` переведён на
  `useSvelteKitNavigation()` вместо ручного `setNavigator` — это и есть эталонная разводка хоста.
* **Playwright e2e-харнесс в ките** (`playwright.config.ts`, `e2e/overlays.spec.ts`, `npm run
  test:e2e`): регрессии оверлеев на демо — Dialog закрывается ✕/Escape/scrim, и `Select` внутри
  `Dialog` при выборе нижнего пункта (рендерится ниже кромки диалога) НЕ рвёт модалку. Прогон в CI:
  job `e2e` в GitHub Actions гейтит деплой демо и бежит на PR.
* **Демо-стили: подключён кит-барель `styles/index.css` вместо ручного списка.** Демо-`app.css`
  импортировал `tokens`/`typescale`/`page-transitions` руками и пропускал `animations.css` (и
  `utilities.css`) → enter-`@keyframes` оверлеев (`dlg-in-pop`, `dr-slide`, `pop-anim`) не были
  определены, поэтому Dialog/Drawer появлялись БЕЗ входящей анимации, тогда как Svelte `out:`-exit
  играл. Демо-only (`src/lib/**` не тронут), библиотека без изменений.

## 2026-07-17

### Версия 0.5.0

### `Select` переписан на bits-ui-примитив (dismissable-слой) — не роняет родительскую модалку

* **`Select` теперь построен на `bits-ui` `Select` (как `Combobox`), а не самописный body-portal.**
  Раньше меню портировалось в `<body>` «руками» и НЕ участвовало в стеке dismissable-слоёв bits-ui:
  клик по опции, чья Y-координата уходила ниже нижней кромки родительского `<Dialog>` (длинный
  список), bits-ui засчитывал модалке как «клик снаружи» (`isClickTrulyOutside` — координатная
  проверка) и **закрывал модалку**. Симптом: в модалке создания объекта выбор нижнего пункта в
  Select с типом объекта закрывал всю модалку. Теперь `Select.Content` — настоящий bits-ui
  dismissable-слой: он становится верхним «ответственным» слоём, и клик внутри него для модалки
  больше не «снаружи». Причина закрыта по корню, поведение единое с `Combobox`.
* **Публичный API сохранён байт-в-байт:** `value` (bindable, string | string[]), `label`,
  `options: {value,label}[]`, `error` (string | boolean), `placeholder`, `disabled`, `required`,
  `multiple`, `respectBack`, `class`, `onchange?.({ target: { value } })`, спред `...restProps`
  (data-testid) на корневой `<div>`, clear-кнопка (✕) и сводка «N selected». Меню портируется в
  `<body>` через `Select.Portal`, z из overlay-layer (`layer.z + 2`, как `Combobox`), ширина по
  `--bits-select-anchor-width`, `useOverlay` для history-back.
* **Clear (✕) гасит `pointerdown`** (bits-триггер открывается на pointerdown) — сброс значения не
  открывает дропдаун.

## 2026-07-16

### Версия 0.4.2

### `data-tick` на ячейке тика в `ChatListItem`

* **`ChatListItem` проставляет `data-tick={tick}` на контейнере правого рельса.** Test-хук для
  e2e: даёт прочитать состояние тика (`pending`/`sent`/`delivered`/`read`/`failed`) из DOM без
  завязки на иконки. Визуально ничего не меняет.

## 2026-07-15

### Версия 0.4.1

### Уходящая анимация оверлея при overlay-aware навигации (вариант A)

* **Overlay-aware `navigate()` теперь ждёт exit-анимацию оверлея перед сменой роута.** Раньше
  `navigate()` из открытого оверлея флипал `open=false` и СИНХРОННО делал `replace` — хостовая
  страница (вместе с оверлеем) уносилась на полукадре, и оверлей просто мигал (напр. меню чата
  «Открыть бронь»: меню исчезало ровно в момент открытия страницы брони, без анимации). Теперь
  `dismissForNavigation()` возвращает max exit-длительность закрытых оверлеев, и `navigate`
  придерживает `replace` на это окно — уходящая анимация успевает проиграть.
* **Длительность декларируется оверлеем.** `useOverlay(getOpen, close, { exitMs })` →
  `registerOverlay({ exitMs })`. `Popover` — 120ms (`popOut`), `Dialog` — 220ms (`dialogPop`) /
  260ms (fullScreen `sheet`); прочие — дефолт 200ms. `prefers-reduced-motion` → навигация без
  задержки (анимации и так 0ms).

### Оверлеи «правильно по умолчанию» — portal + z + history-back + overlay-aware навигация

Единый примитив для всех scrim-оверлеев: `registerOverlay`/`useOverlay` берёт на себя
portal, z-band из глубины стека и участие в history-back — раньше это цеплялось руками на
каждом колл-сайте.

* **`registerOverlay()` + хук `useOverlay(getOpen, close, { respectBack })`** (`router/overlay`,
  `hooks/useOverlay.svelte`). Регистрация оверлея в LIFO-стек: возвращает reactive `z`
  (scrim `z`, content `z+1`, вложенный попап `z+2`; BASE 60/STEP 10), кладёт синтетическую
  history-запись (Back закрывает верхний), и снимает токен на unmount без лишнего `history.back`.
* **Back закрывает scrim-оверлеи по умолчанию.** Участвуют: `Dialog`, `BottomSheet`, `Drawer`,
  `Lightbox`, `VideoLightbox` (через `Dialog`), `DropdownMenu`, `Popover` (в `modal`-режиме),
  дропдауны `Select`/`Combobox`/`DatePicker`/`DateRangePicker`. **`Tooltip` и hover-`Popover`
  (без scrim) — НЕ участвуют.** Opt-out на компоненте: `respectBack={false}`.
* **Ленивый self-init перехватчика.** `registerOverlay` идемпотентно ставит back-interceptor на
  первом открытии — любой хост с кит-роутером получает back-закрытие без ручного
  `initOverlayStack()` (он остаётся как явный opt-in для `ModalOutlet`/SvelteKit-адаптера).
* **Overlay-aware `navigate()` (вариант A) + `keepOverlays`.** При открытых оверлеях
  `navigate(to)` закрывает их и заменяет верхнюю синтетическую запись назначением — оверлей
  исчезает, одинарный Back возвращает на исходный экран (убирает костыль «навигировать в
  onDidDismiss»). `navigate(to, { keepOverlays: true })` — `push` ПОД оверлеем для restore-on-back
  паттернов (шторка карты). `dismissForNavigation()` в стеке.
* **Portal-в-`<body>` по умолчанию.** `Popover` — дефолт `portal={true}` (было `false`; opt-out
  `portal={false}` для inline hover). `DropdownMenu` переписан с inline `absolute` на portal +
  fixed-позиционирование от триггера (больше не клипается scroll/overflow-предком).
* **z вложенных порталов — автоматический.** `provideOverlayZ` публикует z оверлея потомкам:
  `Select`-внутри-`Dialog` сам ложится поверх диалога, без ручного проброса `getOverlayLayer()`.
  Fallback 60 (дропдаун над контентом диалога даже без явного z).

### Исправлено

* **overlay-stack больше не рассинхронизируется с роутером.** Module-level default-инстанс был
  захардкожен на `browserHistoryAdapter` в момент импорта; теперь читает активный адаптер из
  `router/history/registry` (`getHistory()`) лениво — в SvelteKit/Meteor-хостах overlay-back и
  роутер работают на одном источнике истории.
* Методы `createOverlayStack` — closure-based (без `this`): detached-экспорт `openOverlay` из
  барреля больше не бросает при standalone-вызове.

### Breaking-behavior

* **Standalone-оверлеи теперь закрываются по Back** (раньше — нет). Ожидаемо и является целью;
  требует регресс-прохода у потребителей (admin/cabinet/spaces).
* **z standalone `Dialog` сдвинулся с классовых `z-40/z-50` на инлайновые `60/61`** (из слота
  стека). Элемент, намеренно припаркованный между `z-50` и `z-60` (напр. тост над диалогами),
  теперь окажется ниже — поднимите его выше `60`.

### Версия 0.3.12

### Исправлено (chat — нажатие на медиа больше не открывает лайтбокс И меню разом)

* **Медиа-сообщение: одно нажатие — одно действие.** Долгое нажатие (тач) / правый клик по фото
  или видео открывали контекстное меню сообщения **и** просмотрщик одновременно. Причина: меню
  висит на двух путях — `onclick` строки (мобильный `menuOnClick`) и `oncontextmenu` пузыря
  (десктоп right-click / Android long-press). Медиа-слоты гасили только `onclick`
  (`stopPropagation`), а `contextmenu` — отдельный тип события — беспрепятственно всплывал к пузырю
  и открывал меню, тогда как последующий `click` открывал просмотрщик. Прежний фикс (v0.3.4)
  закрыл лишь путь `onclick`.
* **`createMediaTapGuard(openViewer)`** в `chat/helpers.ts` — общий гвард, разложенный на медиа-кнопку
  (`ImageMedia`, `VideoMedia`): `oncontextmenu` продолжает всплывать (меню открывается), гасит лишь
  нативное меню браузера и взводит флаг; последующий `click` проглатывается — просмотрщик НЕ
  открывается. Обычный тап (без предшествующего `contextmenu`) открывает просмотрщик. `onclick`
  всегда `stopPropagation` — тап по медиа не долетает до tap-to-menu строки. Итог: тап → просмотрщик,
  long-press/right-click → меню, никогда оба. `<a>`-фолбэк получил `oncontextmenu preventDefault`.
* Проверка: `media-tap-guard.test.ts` (юнит-логика) + `media-tap-guard.dom.test.ts` (jsdom, реальный
  Svelte-компайл `media-tap-guard.harness.svelte` с настоящими всплывающими DOM-событиями —
  подтверждает, что delegation чтит `stopPropagation`, `contextmenu` всплывает к меню, trailing
  `click` не открывает просмотрщик).

## 2026-07-14

### Версия 0.3.11

### Изменено (chat — заглушка фото объекта в booking-строке)

* **`ChatListItem`: иконка вместо дефолтной картинки.** Строка booking-диалога без фото объекта
  (или с битым URL) рисует плитку с иконкой `faHouse` (`bg-primary-container`, как fallback у
  `Avatar`) вместо `<img>` на несуществующий `images/property-default.png`. Ошибка загрузки
  трекается per-URL (`onerror`) — обновлённый снапшот с живым фото ретраит `<img>`.
  `data-testid="booking-image-fallback"`.

## 2026-07-13

### Версия 0.3.10

### Добавлено (virtual/chat — холодный вход без мельканий + возврат к прежней позиции скролла)

* **`VirtualList.estimateSize`** — оценка высоты строки для ещё не измеренных строк. Источник
  мельканий ПЕРВОГО входа в чат: virtua стартует с плоского дефолта ~40px, а реальные строки —
  140px-карточки и 300px-фото; каскад «замер видимых → сдвиг → в viewport въехали новые
  немеренные → замер → сдвиг…» не успевает сойтись под settle-cap на слабом CPU, и хвост
  коррекций виден глазами. С оценками первый рендер стоит в почти финальной геометрии — каскада
  нет. Реализация: синтез virtua `CacheSnapshot` (`[sizes[], defaultSize]` — внутренний формат,
  закреплён guard-тестом `cache-shape.test.ts`); measured-значения всегда приоритетнее оценок.
* **Measured-кэш `cacheKey` теперь по id строки (`getKey`), не по индексу** — переживает
  несовпадение глубины окна между визитами (25 свежих строк против 80 загруженных в прошлый раз)
  и накапливается за сессию.
* **`VirtualList`: rekey на переходе empty↔non-empty** (только для списков с
  `name`/`cacheKey`/`estimateSize`): virtua потребляет `cache` один раз при создании стора, а
  async-списки монтируются пустыми — теперь VList пересоздаётся, когда данные пришли, и кэш/оценки
  реально применяются. Заодно чинит offset-restore `name`-списков, монтирующихся пустыми.
* **`MessagesList.positionKey` — возврат туда, где был** (module-память позиций, как sizeCaches):
  на каждом settled-скролле сохраняется `{anchorId, delta, pinned, depth}`; на следующем входе
  сохранённая НЕ-pinned позиция приоритетнее unread divider/низа — anchor-строка возвращается на
  свой пиксель (`scrollToIndex(idx,{align:'start',offset:delta})`). Недостающая история
  догружается СКРЫТО в settle-фазе (extended cap 1500мс, depth-cap 200); anchor удалён/не
  найден или память pinned → прежнее поведение. Экспорт `getSavedPosition`/`clearMessagesPosition`.
* **`VirtualList.findItemIndex`/`getItemOffset`** — публичные passthrough-методы virtua (нужны
  anchor-сохранению).
* **`SlotSet.estimateHeight`** — реестровый хук: высота БАБЛА карточного типа — знание хоста
  (booking-карточка ≈ 140px); кит сам оценивает текст (по длине), медиа (точная формула бокса
  ImageMedia из `meta.width/height`), service/deleted/audio/document.
* **`chat/helpers`: `estimateMessageHeight`/`mediaBoxHeight`/`textBlockHeight`** — чистая
  математика оценки строки (сепаратор даты, unread divider, группировка, reply-квота, бабл) +
  юнит-тесты. `ChatMessageList` собирает всё это и передаёт `estimateSize`+`positionKey`
  (`chat:<chatId>`) в `MessagesList` автоматически — потребителям чата ничего менять не нужно.
* **Кнопка scroll-to-bottom больше не вспыхивает при входе**: `onScrollAwayChange` гейтится на
  `settled` — во время скрытой settle-фазы virtua эмитит scroll-события с промежуточной
  геометрией (mount-time `onscroll(0)` при уже оценённом полном scrollSize читается как «далеко
  от низа»), а кнопка хоста живёт вне скрытой обёртки и висела поверх спиннера. На reveal
  состояние пересчитывается — после restore в середину истории кнопка появляется сразу.
  Кнопке в `ChatMessageList` добавлен `data-testid="chat-scroll-down"`.
* **`VirtualList`: scroll из inert/отсоединённого поддерева игнорируется** — Svelte помечает
  outroing-ветку (уезжающий page-transition слой) `inert`, и её скроллер может схлопнуться в 0
  посреди ухода; такое событие затирало `name`-снапшот нулём, и следующий вход в список открывался
  сверху (репро: список диалогов кабинета — drill-in в чат и назад). Такие события не считаются
  пользовательским скроллом: без сохранения, без reveal скроллбара, без host-callback'а.
* **`ChatListItem`: аватарка поверх фото объекта прижата к низу** — обёртка оверлея была обычным
  `div`, inline-flex аватар вставал на текстовый baseline её line box'а, и strut оставлял ~7px
  зазора под кругом. Обёртка стала flex-контейнером — круг вплотную к нижнему краю фото.

## 2026-07-12

### Версия 0.3.9

### Исправлено (virtual/chat — мобильный вход в чат: пустая пауза и мелькающие коррекции)

* **`VirtualList.cacheKey` — переиспользование измеренных высот строк без restore offset'а**.
  Источник «мелькания» (на телефоне 5–10 видимых прыжков за первую секунду в чатах с высокими
  карточками — booking и т.п.): virtua стартует с ОЦЕНКИ высоты строки (~40px против реальных
  ~140px), позиционирование по `scrollToIndex(last,{align:'end'})` считается из оценок, а
  замена оценок измерениями доезжает уже ПОСЛЕ reveal (на throttled-CPU — сериями), и каждая
  коррекция ложится кадром позже видимого сдвига. `cacheKey` (для чата — `chat:<chatId>`)
  сохраняет measured-cache virtua при каждом scroll-событии и отдаёт его при следующем маунте
  того же ключа: повторный вход стартует с измеренных высот — коррекций просто нет. В отличие
  от `name`, offset НЕ восстанавливается (позицией владеет хост — прижим к низу/unread divider).
  Прямую запись `scrollTop` (в обход API virtua) пробовали — его отложенная компенсация
  рассинхронизируется и осциллирует; позиционирование остаётся только через `scrollToIndex`.
* **`MessagesList.onSettledChange` + спиннер в `ChatMessageList`**: пока идёт скрытая settle-фаза
  (первые данные + сходимость измерений) панель списка была визуально пустой — на мобильном до
  ~1с «страница не рисуется». Теперь `ChatMessageList` показывает центрированный `Loading` пока
  список скрыт (и во время initial fetch); genuinely пустой чат спиннер не показывает
  (len 0 + history exhausted).
* **`ChatSession.chatId`** — сессия отдаёт свой chatId (нужен для `cacheKey`, полезен хостам).

### Версия 0.3.8

### Исправлено (navigation — страховка PageTransition от зависших анимационных часов)

* **`PageLayer`: wall-clock фолбэк на enter и exit** (`SETTLE_GRACE_MS`). CSS-анимация входа и
  rAF-овый `out:hold` идут только пока рендерятся кадры: в throttled-окружении (headless,
  фоновая вкладка, забитый main thread) входящий слой замирает посреди слайда (напр. `ptSaInFwd`
  на `translateX(30%)`), а уходящий не размонтируется — повторные навигации СТАКАЮТ замёрзшие
  `pt-layer` (наблюдалось в Playwright e2e кабинета: страница уезжала вправо на 30%/60%).
  `setTimeout` живёт на wall-clock: через `holdMs + 250мс` вход принудительно завершается
  (снимается `pt-in-*` — слой встаёт в settled-состояние), а всё ещё смонтированный уходящий слой
  прячется (`visibility:hidden` + `pointer-events:none`; сам узел сносит Svelte, когда его цикл
  оживёт — вручную не удаляем). В здоровом окружении оба колбэка — визуальные no-op'ы:
  завершённая enter-анимация (fill backwards) даёт то же computed-состояние, что и отсутствие
  класса, а завершённый exit уже размонтирован.
* **`PageLayer`: воскрешение слоя (Svelte BranchManager resume)**. Svelte 5.55 держит уходящую
  ветку `{#key}` в onscreen-карте до конца outro; если её ключ возвращается раньше (тот же чат
  переоткрыт, пока зависший outro всё ещё держит старый слой) — новая ветка НЕ создаётся, живой
  страницей становится тот же элемент (`resume_effect`). Единственный синхронный сигнал —
  `outro.reset()` → `tick(1)` при уже снятом Svelte'ом `inert` (честный outro-кадр тоже даёт
  t=1, но там `inert === true`). По нему слой восстанавливается: снимаются `pt-out-*`, инлайновые
  `visibility/pointer-events` страховки, отменяется её отложенный hide. Сопутствующее: sweep
  старших соседей не трогает не-inert (то есть воскрешённые, живые) слои; ветки neutralize/heal
  снимают `inert` с предка, который хостит живой контент — иначе страница нетапабельна.

### Версия 0.3.4

### Исправлено (`Select` — выпадающее меню поверх диалога, кликабельно)

* **`Select` порталит своё меню в `<body>`** вместо `absolute … z-50` внутри панели. Раньше меню
  обрезалось родительским `Dialog` (панель `overflow-hidden`, тело `overflow-y-auto`) — нижние опции
  были недостижимы. Теперь меню — `position:fixed` от `getBoundingClientRect()` триггера (репозиция на
  `scroll`(capture)/`resize`, пока открыто), z-index = `getOverlayLayer()?.z + 2` (над своим диалогом,
  под более глубокой модалкой), без слоя — прежний `z-50` (обратно совместимо).
* **Критично — `pointer-events:auto` на меню И его backdrop.** bits-ui `Dialog` на время открытия
  ставит `body { pointer-events:none }`; порталенный в `<body>` потомок это наследует → меню рисуется
  поверх (z корректный), но клики проваливаются сквозь него в диалог. Симптом выглядит как z-баг, но
  z в порядке. Меню и его backdrop теперь явно включают `pointer-events`.
* `getOverlayLayer` экспортирован из барреля `apartx-ui/overlays`.

## 2026-07-11

### Версия 0.3.7

### Исправлено (chat — детерминированная начальная позиция скролла: intent-based pin + settle-фаза)

* **`pinned` больше не выводится из геометрии скролла** (`MessagesList`). Прежняя модель (v0.3.6 и
  раньше): «прилип к низу» вычислялся из offset/scrollSize в каждом onscroll — а во время загрузки
  геометрия врёт: любой поздний reflow (декод картинки, постер, шрифт, клавиатура) на мгновение
  читается как «не внизу», эвристика снимала залипание и гонялась с собственным репином — отсюда
  «разброс» начальной позиции. Теперь `pinned` меняет ТОЛЬКО пользовательский жест (wheel / touch /
  скроллбар / клавиши — «окно жеста» открывается input-событием, продлевается scroll-событиями,
  закрывается `onscrollend`/тайм-аутом) либо императивный `stick()`. Пока pinned — ЛЮБОЕ изменение
  мира (рост контента, ресайз вьюпорта, append строк) безусловно репинит к низу: гонка исчезает по
  построению.
* **Settle-фаза на открытии**: от первых данных до стабилизации высоты контента (3 стабильных кадра,
  жёсткий cap 400мс) список скрыт `visibility:hidden` (layout и измерения virtua работают), каждый
  ресайз безусловно повторяет целевой скролл (низ или unread-divider). Пользователь видит чат сразу
  в правильной позиции, без видимых прыжков. Жест пользователя во время settle немедленно завершает
  фазу (его намерение важнее). Чанковые загрузки (offline-first сторы) во время settle
  переасссертят цель, а не дёргают список.
* **`VirtualList.onContentResize` теперь ловит и ресайз вьюпорта** (ResizeObserver на скроллере в
  дополнение к контент-диву): экранная клавиатура/поворот при pinned репинят к низу.
* **Медиа-слоты резервируют бокс всегда** (`ImageMedia` 4:3, `VideoMedia` 16:9 при неизвестных
  размерах, `object-fit:cover`) — вместо интринсик-высоты, которая рефловила строку при загрузке
  байтов. Стабильные высоты строк = детерминированная виртуализация у корня (как Telegram/WhatsApp).

### Версия 0.3.6

### Исправлено (chat — скролл больше не «уезжает» от низа при загрузке медиа)

* **Stick-to-bottom переживает поздний ресайз контента** (`VirtualList` + `MessagesList`). Корень
  «случайной позиции скролла на большом чате»: медиа-строка (фото/видео) домеряет реальную высоту
  ПОСЛЕ того, как байты загрузились — уже после того, как начальный скролл к низу «устаканился».
  virtua при таком росте сохраняет offset (top-anchored), поэтому вьюпорт молча съезжает вверх от
  низа и застревает в произвольном месте. Проверено в demo: у низа (`fromBottom 0`) → строка растёт
  на 300px → `fromBottom 264` и застревало; с фиксом — репин к низу за ~80мс.
* **`VirtualList` наблюдает высоту контента** через `ResizeObserver` на внутреннем контейнере virtua
  (обёртка `display:contents` даёт доступ к элементу, не влияя на измерение вьюпорта) и зовёт новый
  проп `onContentResize`. `MessagesList` по нему **репинит к низу, только пока залипание активно**
  (`ready && shouldStick`) — если пользователь прокрутил вверх, ресайз его НЕ дёргает вниз (проверено).
  Без петли: репин меняет offset, а не высоту контента.
* **Demo мокает реальный read-watermark** (`lastReadSeq`) — как настоящий потребитель; без него все
  входящие считаются непрочитанными и якорь схлопывается в первую входящую строку.

### Версия 0.3.5

### Исправлено (chat — детерминированный скролл при входе в чат)

* **Единый драйвер начального позиционирования** (`MessagesList.svelte`). Раньше позицию задавали
  ДВА конкурента: `ChatMessageList` дёргал `scrollToIndex` к разделителю непрочитанного, а
  `MessagesList` независимо липнул к низу — каждый новый `scrollToIndex` **отменяет коррекцию
  предыдущего** (планировщик ядра virtua), поэтому на большом чате при повторном входе скролл
  вставал в случайном месте (~3 из 5 раз). Теперь позиционирует ровно один раз `MessagesList` через
  новый проп `initialIndex` (разделитель непрочитанного, иначе низ); `ChatMessageList` только
  прокидывает индекс и больше сам не скроллит.
* **`onLoadOlder` заблокирован до позиционирования** (гейт `ready`). На маунте virtua эмитит
  `onscroll(0)`, что раньше триггерило подгрузку старых сообщений (`offset < 100`) и сдвигало
  индексы прямо во время сходящегося начального скролла. Гейт снимается после `initialize()`.
* **Без самодельных циклов.** Опора на нативную самокоррекцию `scrollToIndex` virtua (переприменяет
  цель на каждое измерение строки, пока видимый диапазон не измерен) — никаких rAF/settle-петель не
  добавлено, форсированных reflow нет.

### Версия 0.3.4

### Исправлено (chat — тап по медиа больше не открывает меню сообщения на мобиле)

* **Тап-таргет контекстного меню расширен с пузыря на всю строку** (`Message.svelte`). При
  `menuOnClick` (мобильный режим) `onclick=openMenu` теперь на строке-контейнере, а не на пузыре
  `max-w-[80%]` — тап по маленькому текстовому пузырю **или напротив него** (в пустом гаттере строки)
  открывает меню, «как будто сообщение на всю ширину» (Telegram-подобно). `oncontextmenu` (right-click,
  десктоп) остаётся на пузыре. Удалённые сообщения меню не открывают (гард `if (removed) return`).
* **Медиа-слоты перехватывают свой тап** (`VideoMedia`, `ImageMedia`) — `e.stopPropagation()` в
  onclick кнопки просмотрщика/плеера и в `<a>`-fallback. Раньше тап по фото/видео всплывал до пузыря и
  открывал меню вместо просмотрщика. Теперь тап по медиа = просмотрщик, тап по гаттеру рядом = меню.

### Версия 0.3.3

### Исправлено (image Lightbox закрывается по нативному history back)

* **`Lightbox` (viewerjs) тоже регистрируется в overlay-stack** — та же правка, что и у
  `VideoLightbox` в 0.3.2. viewerjs управляет своим z/ESC/бэкдропом, но про history ничего не знает,
  поэтому нативный/браузерный BACK **менял страницу вместо закрытия лайтбокса**. Теперь `Lightbox`
  сам зовёт `openOverlay`/`closeOverlay` и ставит interceptor через `initOverlayStack()` на mount.

## 2026-07-10

### Версия 0.3.2

### Исправлено (chat — VideoLightbox закрывается по нативному history back)

* **VideoLightbox сам регистрируется в overlay-stack.** Kit `Dialog` интегрируется с overlay-stack
  (синтетическая history-запись + закрытие по back) **только внутри `<ModalLayer>`** — эффект
  гейтится `if (!layer) return`, поэтому standalone-Dialog (как VideoLightbox) не пушил запись и
  нативный/браузерный BACK **менял страницу вместо закрытия видео**. Теперь VideoLightbox сам зовёт
  `openOverlay`/`closeOverlay` и ставит interceptor через идемпотентный `initOverlayStack()` на mount
  — back закрывает плеер независимо от того, смонтирован ли у хоста `<ModalOutlet>`.

### Версия 0.3.1

### Изменено (chat — VideoLightbox без шапки диалога)

* **Убрана шапка Dialog у `VideoLightbox`** (`title=""`, `showCloseButton={false}`) — полноэкранное
  видео без диалоговой шапки. Вместо неё — плавающий крестик поверх видео (top-right); ESC и
  нативный history back по-прежнему закрывают.

### Версия 0.3.0

### Добавлено (chat — телеграм-видео)

* **`VideoMedia` — новый слот для `video`.** Тайл в ленте: постер-превью + play-кнопка по центру
  + бейдж длительности; файл видео не грузится, пока не тапнули. Бокс резервируется из
  `width`/`height` (лента не переверстывается при загрузке постера). Читает
  `meta.file.{url,posterUrl,width,height,duration}`. `media-slots.ts`: `video` теперь → `VideoMedia`
  (был `ImageMedia`). Экспортится из `apartx-ui/chat`.
* **`VideoLightbox` — полноэкранный плеер** (`apartx-ui/lightbox`). Построен на kit `fullScreen`
  `Dialog`, поэтому регистрируется в `overlay-stack`: закрытие по **нативному history back**
  (кнопка «назад»/свайп) приходит из коробки, плюс ESC/тап-вне/X. Контролы — `media-chrome`
  (web-components, лениво регистрируются на mount, SSR-safe), скин темизирован под M3 через
  CSS-переменные. `media-chrome` добавлен в `peerDependencies`.
* **`formatDuration(sec)`** — хелпер для бейджей длительности (`m:ss` / `h:mm:ss`), покрыт юнит-тестом.
* **Демо:** витрина переведена на kit `VideoMedia`/`VideoLightbox`; демо-`VideoSlot` удалён,
  видео-мок приведён к реальной shape меты.

Требует бэкенд: сервер отдаёт `meta.file.posterUrl` (apartx-server — разворот `posterFileId`).

### Версия 0.2.19

### Исправлено (демо чата — медиа тянется на всю ширину пузыря)

* **Медиа теперь `w-full min-w-[300px]`** вместо фиксированных 300px: если подпись растягивает
  пузырь шире 300px, картинка/видео растут вместе с ним, а не висят у́же сообщения.
* **Само медиа — `w-0 min-w-full`.** Это убирает его intrinsic-ширину (0 у `<img>` до загрузки,
  480 после; 300×150 у `<video>` до метаданных, 1280×720 после) из расчёта shrink-to-fit пузыря.
  Иначе `w-full` вернул бы скачки из 0.2.16: пузырь снова сайзился бы от загруженного медиа.
  Ширину в раскладке даёт `min-w-full`, высоту — `aspect-ratio` из меты.

### Версия 0.2.18

### Исправлено (демо чата — медиа задаёт ширину пузыря)

* **Медиа расширено до 300px** (`w-[300px] max-w-full`) в `MediaSlot`/`VideoSlot` вместо `w-64`.
  После 0.2.17 скачки ушли, но ширину пузыря стала диктовать подпись, и картинка/видео висели
  уже́ сообщения. Теперь, как в телеграме, ширину пузыря задаёт медиа, а подпись переносится
  под ним; `max-w-full` держит медиа в границах на узких экранах.

### Версия 0.2.17

### Исправлено (демо чата — прыжки раскладки на медиа)

* **Медиа в демо получило явные размеры.** `mock-transport.ts` теперь возит `width`/`height`
  вместе с картинками (480×360) и видео (1280×720), а демо-слоты резервируют бокс заранее:
  `MediaSlot` задаёт сетке определённую ширину (`w-64`) и каждой ячейке определённый бокс
  (`aspect-ratio` для одиночной картинки, фиксированная высота строки для галерей), `VideoSlot`
  — `w-64` + `aspect-ratio` из меты.
* **Причина.** Пузырь сообщения — shrink-to-fit, а `w-full`/`max-h-64` не дают определённого
  размера: до загрузки ширину брали из intrinsic-размера (у `<img>` он 0, у `<video>` — дефолтные
  300×150), после загрузки — из реального. Каждая долетевшая картинка переверстывала строку, а
  `virtua` пересчитывала список. Правка только в `src/routes` (демо) — `src/lib` не затронут,
  бампать сабмодуль в кабинете/админке не нужно.

### Версия 0.2.16

### Добавлено (chat — `data-tick` как e2e-хук)

* **`data-tick={tick}` на коробке тика** (`slots/MessageTimeDefault.svelte`). Кабинетная спека
  `chat-delivery.spec.ts` искала `[data-tick]` с самого своего появления (`83f2ab2bc`, v2.0.43), но
  атрибут в ките **не существовал ни в одном коммите** — тест «own-message tick advances
  sent→delivered→read» из-за этого падал всё это время. Атрибут ставится только там, где есть тик,
  то есть у своих сообщений.

---

## 2026-07-10

### Версия 0.2.15

### Исправлено (chat — пузырь дрожал при смене статуса доставки)

* **Под тик зарезервирована фиксированная ширина** (`slots/MessageTimeDefault.svelte`). FontAwesome
  масштабирует значок по пропорции его `viewBox`, поэтому четыре состояния имеют разную ширину:
  `check` 0.875em, `clock` 1em, `triangle` 1em, `check-double` 1.25em. Блок времени с тиком —
  это `float-right` внутри бегущего текста, так что его ширина решает, переносится ли последняя
  строка. При переходе `pending → sent → delivered → read` float менял ширину и пузырь мог
  дёргаться (перенос строки то появлялся, то исчезал). Теперь значок центрируется в коробке
  шириной по самому широкому (`w-[1.25em]`, `shrink-0`), и ширина float'а одинакова во всех
  состояниях. Замер в демо: было `40.9px` (clock) → `39.7px` (check), Δ=1.2px; стало `43.2px` в
  обоих, Δ=0. `check-double` — ровно 12.5px при коробке 12.5px, влезает без сжатия.
  У входящих сообщений тика нет — коробка не рендерится, лишнего отступа не появляется.

---

## 2026-07-10

### Версия 0.2.14

### Исправлено (демо-плейграунд — у времени «пропал» float)

* **Мок-сообщения демо `/chat` не несли `type: 'text'`** (`src/routes/chat/mock-transport.ts`).
  Ровно та же болезнь, что чинилась в `session.send()` в v0.2.13, только в фикстурах: без `type`
  `MessageRenderer` уходит в stacked-ветку (`isText === false`), и время с тиком встаёт отдельной
  строкой под текстом вместо telegram-float в конце последней строки. Затронуты были начальная
  выборка (обычные текстовые + два непрочитанных), ответ мок-`sendMessage` и `injectInbound`.
  Медиа/`booking`/`ai`/`system` свой `type` несли всегда, поэтому выглядели правильно — из-за чего
  и складывалось впечатление, что float «пропал» только у обычных сообщений.
  На `src/lib` не влияет: изменены только фикстуры демо.

---

## 2026-07-10

### Версия 0.2.13

### Исправлено (chat — пузырь дёргался по высоте на время pending)

* **Оптимистичное текстовое сообщение не несло поля `type`.** `MessageRenderer` выбирает раскладку по
  `isText = message.type === 'text'`: без него pending-пузырь проваливался в *stacked*-ветку, где
  время с тиком стоит отдельной строкой под текстом. Серверный эхо-ответ приходит с `type: 'text'` и
  переключает раскладку на telegram-стиль (время `float-right` в конце последней строки) — лишняя
  строка исчезала, и пузырь скачком терял ~13px в высоте прямо в момент отправки.
  `session.send()` теперь ставит `type: 'text'`, как `sendMedia()` всегда ставил `type: input.type`.
  Замер на живом кабинете (сэмплинг 40 мс по последнему пузырю): было `89px → 76px` (Δ=13px),
  стало `76px` без изменений (Δ=0px), причём `span.float-right` присутствует с первого кадра.

---

## 2026-07-10

### Версия 0.2.12

### Исправлено (чат — delivered-acker шумит на непрочитанных каналах)

* **`delivered-acker` пропускает channel-диалоги** (`replication/delivered-acker.ts`). `channel` —
  единственный read-fanout (broadcast) kind: сервер намеренно no-op'ит для него `markDelivered`
  (персональный delivered-watermark на каждого подписчика бродкаста = write-amplification), поэтому
  `lastDeliveredSeq` навсегда остаётся `0`. У **непрочитанного** канала `lastReadSeq < seq` и
  `lastDeliveredSeq = 0` → persisted-watermark guard не срабатывал, а in-memory `acked` живёт лишь на
  время загрузки → клиент слал `Chat.markDelivered` **на каждую перезагрузку**, сервер молча
  выбрасывал. Delivered-тик для бродкаста бессмыслен → теперь такие диалоги отсекаются сразу
  (`d.kind === 'channel'`), без обращения к серверу. Юнит-тест на «непрочитанный канал не акается».

### Версия 0.2.11

### Исправлено (DateRangePicker — портал попапа)

* **`DateRangePicker` использует top-level `Portal`** из bits-ui вместо `BitsRange.Portal`. У
  bits-ui v2 неймспейс `DateRangePicker` **не экспортит `Portal`** (в отличие от `DatePicker`),
  поэтому `<BitsRange.Portal>` резолвился в `undefined` → `ESModulesLinkingWarning` на сборке, а
  поддерево `<Content>` (попап-календарь) дропалось. Импорт `Portal` из `'bits-ui'` + `<Portal>`.

### Версия 0.2.10

### Исправлено (чат — мигание пустого списка диалогов)

* **`liveArray` отдаёт `loaded`** (`replication/live-query.svelte.ts`) — флаг перещёлкивается в
  `true` на первой эмиссии Dexie liveQuery (`next`). `createDialogsStore` пробрасывает `get loaded()`.
  Хост-список теперь отличает «ещё не загрузилось» (стартовый `[]` до первого батча) от «загружено и
  правда пусто» → показывает лоадер до первой эмиссии вместо кадра пустого-стейта.

## 2026-07-09

### Версия 0.2.9

### Добавлено (чат — generic media-слоты в ките)

* **`ImageMedia` / `AudioMedia` / `DocumentMedia` + `mediaSlots`** — рендер медиа-сообщений
  (image/video/audio/document) больше не app-специфичен и живёт в ките (`slots/` +
  `media-slots.ts`). Хосты спредят карту в свой реестр: `setMessageRendererRegistry({ ...mediaSlots,
  ...бизнес-типы })`. Подпись медиа — это обычный `MessageBodyDefault` (инлайн `message.text`),
  отдельного «CaptionBody» нет.

### Изменено (чат — telegram-стиль пузыря медиа/времени)

* **Медиа встык (`fullBleed`)** — `Message.svelte` для типов image/video убирает паддинг пузыря и
  клипует углы (`overflow-hidden`), медиа заполняет пузырь edge-to-edge (`isFullBleedMedia` в
  `helpers.ts`). Audio/document и текст — прежний паддинг-пузырь.
* **Время float'ом на строке текста** — `MessageRenderer` рендерит тело+время в `flow-root` c
  `float:right` и `items-end`: короткий текст/подпись держит время на той же строке справа
  (выровнено вниз), длинный — время перетекает вниз-вправо. Как в Telegram.
* **Время оверлеем на медиа без подписи** — чип на скриме (`bg-black/45`) в правом-нижнем углу
  (`MessageTimeDefault` получил проп `overlay` — белый текст/тик).
* Пустой хедер (reply/имя автора отсутствуют) больше не рендерит паддинг-обёртку над медиа —
  ушла полоска фона сверху.

## 2026-07-08

### Версия 0.2.8

### Изменено (чат — компактнее строка списка диалогов)

* **`ChatListItem`: убран `min-h-[69px]`** с обеих строк (пустой заглушки и основной) —
  высота теперь по контенту. **Аватар уменьшен до `size-12`** (48px, было `size="lg"` 56px;
  `class="size-12"` через tailwind-merge перебивает `w-14 h-14`).

### Версия 0.2.7

### Изменено (чат — время сообщения выровнено вправо)

* **`MessageTimeDefault` прижимает строку времени к правому краю бабла** — корневой контейнер
  `inline-flex` → `flex justify-end`. В `MessageRenderer` слоты лежат в `flex flex-col`, поэтому
  `flex` растягивает строку на ширину бабла, а `justify-end` уводит время + галочки + иконку канала
  вправо. Раньше время липло к левому краю.

### Версия 0.2.6

### Изменено (чат — иконка мессенджера зеленеет только на `read`)

* **`MessageTimeDefault` красит иконку канала в `text-success` только при `state === 'read'`.**
  Раньше зелёным подсвечивались и `delivered`, и `read` — теперь `delivered` остаётся серым
  (`text-on-surface-variant`), `read` — зелёный, `failed` — `text-error`. Совпадает с ожиданием
  «зелёная галка = прочитано». Тултип `whatsapp: <state>` не меняется.

### Версия 0.2.5

### Добавлено (чат — helper `chatImageGallery` для лайтбокса медиа-сообщений)

* **`chatImageGallery(messages)`** — упорядоченный список просматриваемых картинок
  (`{ src, alt }[]`) для лайтбокса чата: берёт только сообщения `type:'image'` с уже загруженным
  `meta.file.url` (пропускает видео и ещё грузящиеся optimistic-отправки с одним preview-blob).
  `src` совпадает с тем, что рисует `ImageMedia`, поэтому URL кликнутой миниатюры индексируется в
  этот список напрямую. Хост (кабинет/админка) монтирует один kit `Lightbox` (`apartx-ui/lightbox`,
  viewerjs) на панель и прокидывает `openLightbox(src)` в slot-контекст; галерея снимается снапшотом
  в момент открытия, чтобы приходящие вживую сообщения не пересобирали открытый вьювер. 3 unit-теста.

### Версия 0.2.4

### Добавлено (чат — оптимистичная отправка медиа через kit-сессию)

* **`session.sendMedia(input)`** — оптимистичная отправка картинок/видео/документов симметрично
  `send()`: сразу показывает пузырь (`sendState: 'sending'` + `meta.uploadProgress` растёт по мере
  загрузки), затем свопает на серверное сообщение. Загрузку выполняет хост через новый **опциональный
  seam `ChatTransport.sendMedia(draft)`** (kit не знает про Meteor/загрузчик): хост качает файл, дёргает
  `draft.onProgress(0..1)`, персистит сообщение и возвращает его. `createReplicatedTransport` пробрасывает
  seam через новую опцию `sendMedia`. Новые типы `MediaDraftInput`/`OutgoingMediaDraft`; helper
  `patchMessageMeta` в редьюсере обновляет прогресс на optimistic-сообщении. Тесты (session): пузырь →
  прогресс → своп, fail-путь, отсутствие seam. Заменяет легаси write-путь `MessagesStore.sendImage()` в
  кабинете и админке.

### Версия 0.2.3

### Добавлено (чат — общий `ChatListItem` + унификация статуса доставки)

* **Новый `ChatListItem`** (`chat/ChatListItem.svelte`) — общий ряд списка чатов: аватар (картинка
  брони с оверлеем профиля, либо аватар профиля), имя + маркеры роли/брони, превью последнего
  сообщения (строка отправителя в группе + превью + опциональное предупреждение) и правый рельс с
  **4-state тиком доставки** (⏱→✓→✓✓серых→✓✓синих через `counterpartReadSeq`/`counterpartDeliveredSeq`),
  временем, чипом непрочитанного и иконкой источника-мессенджера. Kit-агностичен: весь текст —
  пропсы (`displayName`/`senderLabel`/`previewText`/`timeLabel`/`warningText`/`missingText`), хост
  считает i18n сам. Заменяет локальные копии в кабинете (`ChatsListItem`) и админке (инлайн-ряд), у
  которых статус в списке был рассинхронизирован (админка рисовала легаси 2-state).
* **`MessageTimeDefault` теперь рисует иконку источника на входящих** (WhatsApp/Telegram/OTA) — раньше
  только каналы своих сообщений. Хостам больше не нужно переопределять `time`-слот ради этого.
* **Новый общий `MessengerIcon`** (`chat/slots/MessengerIcon.svelte`) + helper `messengerKey(message)`
  — маппинг `meta.messenger`/`meta.fromMessengerType` → бренд-иконка (whatsapp/telegram/airbnb/
  booking.com/channex). Переиспользуется `MessageTimeDefault` и `ChatListItem`.
* **`longpress`-экшен вынесен в kit** (`hooks/useLongPress.svelte.ts`) — iOS-safe long-press для
  рядов списка (Android даёт `contextmenu`, iOS WebKit — нет). Экспортирован из `apartx-ui/hooks`.
* **`Message` (пузырь диалога) получил `data-testid="chat-message"` + `data-message-id`** на внешнем
  ряду — паритет с дореестровым кабинетным `Message.svelte`, чтобы общие e2e (`chats-list`,
  `chat-delivery`) находили сообщения в диалоге на kit-рендере.

## 2026-07-07

### Версия 0.2.2

### Добавлено (репликация чата — персистентный per-chat checkpoint сообщений)

* **`replicated-transport.ts` хранит checkpoint в Dexie `_replicationMeta`.** Раньше на каждый
  page-load создавался новый транспорт с `cursorAt=undefined`/`historyExhausted=false`, поэтому даже
  на уже загруженном чате шло ~3 пустых `ChatMessage.findAllNew` (перевыборка новейшей страницы +
  probe истории). Теперь два ключа, по образцу репликации диалогов:
  * `msgTail:<chatId>` — forward-курсор `(updatedAt,_id)`: `tailSync` резюмится с него → один
    инкрементальный (обычно пустой) check вместо перевыборки новейшей страницы с `since=undefined`.
  * `msgHistory:<chatId>` — `{ oldestAt, done }`: backward-прогресс + флаг исчерпания истории;
    пишется после КАЖДОЙ старой страницы, поэтому оборванный backfill (сеть/закрытие вкладки)
    возобновляется с места остановки, а не с новейшей страницы. `fetchOlder` на тёплом кеше не бьёт
    в сеть.
  Итог: тёплый reopen ~3 → ~1 `findAllNew`, контент не перекачивается. Чтение/запись best-effort
  (try/catch), фолбэк к прежнему full-window на свежем IndexedDB. +3 теста (tail-resume,
  exhausted-resume, backward-progress).

### Исправлено (репликация чата — устранён reload-storm `Chat.pullDialogs`/`::chats`)

* **`delivered-acker` гейтит по персистентному watermark.** На загрузке пустой in-memory `acked`
  плюс `uptoSeq = seqCounter` делали серверные ack-и неидемпотентными → безусловный `::chats`
  self-push → бесконечный цикл `Chat.pullDialogs`. Теперь ack только когда новейшее сообщение — от
  собеседника И `max(lastReadSeq, lastDeliveredSeq) < seq` (прочитано ⇒ доставлено; `lastReadSeq` —
  единственный watermark, растущий для channel/broadcast). +тесты (в т.ч. `delivered-loop.test.ts`).
* **`createReadFlusher` получил high-watermark `flushed`** — defense-in-depth от симметричного
  read-path storm (идемпотентность `markRead`). +тесты.

## 2026-07-05

### Версия 0.2.1

### Добавлено

* **Слой read-watermark в репликации чата (B3).** `read-state.ts` (`isReadByMe`) — единый предикат
  «прочитано мной»: для событийных сообщений с `seq` через `seq <= lastReadSeq` (per-user watermark),
  фолбэк на легаси `read[]` только для дособытийных сообщений без `seq`. `lastReadSeq` прокинут в
  `session.svelte.ts`, `ChatMessageList.svelte`, `Message.svelte` (гейт read-on-render), `types.ts`,
  `helpers.ts`. Тесты `read-state.test.ts` + `read-state-live.test.ts` (vitest, зелёные).

### Версия 0.2.0

### Добавлено

* **`apartx-ui/sync` (движок репликации) + слой репликации чата** (`createChatReplication`,
  `ReplicatedTransport` за существующим `ChatTransport`, `createDialogsStore`). Сессия/reducer
  не тронуты.

## 2026-07-04

### Версия 0.1.44

### Добавлено

* **Анимация появления и закрытия `Popover`.** Плавающая поверхность (контекстное меню сообщения,
  дропдауны и т.п.) теперь и появляется, и закрывается с fade + лёгким scale относительно угла-якоря,
  а не мгновенно — по образцу `Dialog`. Обе фазы — чистый CSS, управляемый нативным для bits-ui
  атрибутом `data-state` (`open`/`closed`) на `Popover.Content`: класс `pop-anim` (styles/animations.css)
  вешает keyframe `popIn` на `[data-state='open']` и `popOut` на `[data-state='closed']`.
  * **Почему без forceMount/`out:`-транзишна:** presence-менеджер bits-ui сам держит элемент в DOM после
    закрытия, пока не завершатся его CSS-анимации (ждёт `getAnimations()`), и лишь затем размонтирует —
    поэтому exit реально проигрывается, а внутренний lifecycle bits-ui не рвётся (ручной `forceMount`+`child`
    ронял `derived_inert`-варнинги из text-selection-layer).
  * `transform-origin: var(--bits-popover-content-transform-origin)` — меню растёт/сжимается к своему
    триггеру. Enter c `animation-fill-mode: backwards` (нет вспышки на первом кадре iOS), exit — `forwards`
    (держит faded-out состояние до размонтирования).
  * `transform` на Content безопасен: bits-ui держит позиционирующий `transform: translate()` на
    wrapper-элементе, а не на Content. Обе фазы уважают `prefers-reduced-motion`.

### Версия 0.1.43

### Исправлено

* **Фокус в поле ввода при ответе (reply).** Когда host вызывает `composer.setReply(...)` (например из
  контекстного меню сообщения), `MessageInput` теперь автоматически фокусирует textarea — можно сразу
  печатать, не кликая в поле. Эффект завязан на `replyTo`, снятие ответа фокус не перехватывает.

### Версия 0.1.42

### Исправлено

* **Разделитель «Новые сообщения» больше не мигает на каждое входящее в открытом чате.** `ChatMessageList`
  брал якорь разделителя из живого `firstUnreadId(messages)`, поэтому при открытом чате новое сообщение на
  миг помечалось непрочитанным (разделитель прыгал на него) и через тик пропадал — сообщение читается на
  рендере. Теперь якорь **замораживается в `session.open()`** (`ChatSession.unreadAnchorId`) из состояния
  входа: разделитель показывает лишь то место, где вы остановились при открытии, и не гоняется за живыми
  сообщениями. Свежая сессия (повторный вход в чат) пересчитывает якорь.

### Версия 0.1.41

### Исправлено

* **Порядок сообщений: сервис/системные заметки больше не съезжают в конец чата.** `reducer` сортировал
  по `seq`, а любое сообщение без `seq` (сервисные — `member_join`, закрытие тикета и т.п. — вставляются
  в обход event-log append) получало ключ ≈`MAX_SAFE_INTEGER` и уезжало в хвост после всех обычных
  сообщений, даже свежеотправленных. Теперь сортировка **хронологическая по `createdAt`** (`seq` — только
  тайбрейк для одинаковых меток времени); в хвост принудительно уходят лишь по-настоящему оптимистичные
  сообщения (с `sendState`), чтобы не прыгали выше подтверждённых при перекосе клиентских часов.
  Сервис-сообщения теперь стоят на своём месте во времени. Добавлен тест на интерливинг.

## 2026-07-01

### Версия 0.1.40

### Исправлено

* **`MessageInput`: отправка не ломается при подмене `session`.** `composer` теперь `$derived(session.composer)`,
  а не захваченный один раз `const`. Если хост пересоздаёт сессию (например после join в чат), textarea писала
  в старый (disposed) composer, а `session.send()` читал новый пустой — отправка молча превращалась в no-op до
  перезагрузки. Теперь ввод и `send()` всегда работают с composer'ом текущей сессии.

### Версия 0.1.39

### Изменено

* **`ChatMessageList`: нижний отступ у списка без поломки виртуализации.** Зазор (`pb-2`) навешивается
  на **последний item** (его высоту virtua измеряет), а не на скролл-вьюпорт — иначе `getScrollSize()`
  раздувается и ломается детект stick-to-bottom / scroll-away. `scrollToIndex(last, align:'end')`
  просто выравнивает нижний край padding'а по низу вьюпорта.

### Версия 0.1.38

### Изменено

* **`ChatMessageList`: горизонтальный отступ `px-2` по умолчанию.** Прокинутый потребителем `class`
  теперь мёржится поверх дефолта через `cn`/tailwind-merge (`cn('px-2', className)`), а не заменяет
  его — конфликтующая утилита потребителя выигрывает (`px-4`/`px-0` переопределяют). Раньше отступ
  приходилось задавать в каждом потребителе.

### Версия 0.1.37

### Изменено

* **Групповые пузыри: скругление углов по началу/концу серии.** `Message` получил соседа `next`
  и вычисляет `isGroupEnd` (helper `groupEnd`); у пузыря на стороне отправителя (справа у своих,
  слева у чужих) квадратится верхний угол, когда сообщение не первое в серии, и нижний — когда не
  последнее (`bubbleRadius`). Серия сообщений от одного автора читается как единый стек, а не набор
  одинаковых «капель». Флаг `isGroupEnd` также прокинут в слоты рендерера.

### Версия 0.1.36

### Изменено

* **Групповой чат: подпись автора у входящих сообщений.** Рендерер прокидывает в слоты флаги
  `isGroupStart`/`isGroupEnd`, а дефолтный header-слот показывает `authorName` только в начале
  визуальной группы у чужих сообщений (`authorName && isGroupStart && !me`) — раньше имя рисовалось
  на каждом сообщении при непустом `authorName`. DM-хост просто не заполняет `authorName`. Хосты со
  своим header-слотом (например админка) теперь получают `isGroupStart`, чтобы восстановить подпись
  отправителя в группах.

### Версия 0.1.35

### Добавлено

* **Кнопка «прокрутить вниз» в `ChatMessageList`.** FAB (chevron-down) в правом нижнем углу
  списка — появляется, когда прокрутили вверх дальше порога от низа, с бейджем количества
  непрочитанных входящих (`countUnread`); клик возвращает к низу и включает stick-to-bottom.
  `MessagesList` получил сигнал `onScrollAwayChange` (порог `scrollAwayThreshold`, по умолч. 100px).

### Версия 0.1.34

Новый модуль **`apartx-ui/chat`** — переиспользуемое ядро чата (Svelte 5, без Meteor/i18n) и его shell; выносится из cabinet/admin ради единого чат-клиента. Плюс серия фиксов паритета и QA по ходу адаптации в `apartx-admin` (0.1.27 → 0.1.34).

### Добавлено

* **Чат-ядро `apartx-ui/chat`.** Reducer оконной модели сообщений за швом `ChatTransport`
  (`fetchOlder`/`subscribeLive`/`sendMessage`/`markRead`); `ChatSession` (subscribe-then-fetch,
  оптимистичная отправка, `loadOlder`, `markRead`); slice `composer` (draft + reply-snapshot +
  debounced-persist через `DraftStore`); реестр рендереров по типу сообщения
  (`setMessageRendererRegistry`/`setDefaultSlots`/`registerChatDefaults`) — зеркало modal-registry;
  i18n-шов (`setChatI18n`) и чистые helpers (`deliveryTick`, группировка, `showDate`). Покрыто
  vitest, ноль зависимостей от Meteor/i18next.
* **Svelte-shell чата.** `ChatMessageList` (виртуализация поверх `virtua`, read-watermark с
  debounce, дивайдер непрочитанного, начальный скролл к непрочитанному), `Message` (группировка,
  дивайдеры даты/непрочитанного, hook контекстного меню `onContextMenu`/`menuOnClick`),
  `MessageInput` (autosize-textarea, Enter=отправка / Ctrl-Enter=перенос, `onTyping`, snippet'ы
  `leading`/`gate`, превью вложений), `MessageRenderer` (равномерный набор slot-пропсов +
  вычисляемый per-message `me`).
* **Шов `setSlotContext`** — хост инжектит непрозрачный объект, который раскладывается во все
  slot-компоненты, чтобы бизнес-рендереры получали `t`/`peer`/`chat` без связывания kit с Meteor.
* **`ChatSessionOptions.authorNameOf`** — резолвер имени автора для превью reply.
* **Центрированные сервисные сообщения (`type==='service'`)** — отдельная ветка в `Message`:
  системная заметка по центру (текст переводит хост), без пузыря.
* **`class`-passthrough у `ChatMessageList`** — прокидывается на скролл-контейнер (например `px-2`).

### Изменено

* **`markRead` принимает watermark-сообщение** (seq-or-createdAt), а не числовой `uptoSeq` —
  совместимо с сервером без `seq`.
* **Кнопка отправки — только иконка** (paper-plane); `sendLabel` теперь `aria-label`/`title`.

### Исправлено

* **Скроллбар не показывается на программных скроллах.** `VirtualList` больше не «раскрывает»
  нативный скроллбар при stick-to-bottom / scroll-to-index на монтировании — только на реальном
  пользовательском скролле (счётчик программных скроллов). Аналогично `Content`: программный
  scroll-restore (возврат назад к сохранённой позиции) флагает узел `__scrollRestoring`, а
  обработчик раскрытия его игнорирует.
* **Реактивность composer.** Черновик и reply читаются через `$derived` — программный
  `setDraft`/`setReply` (быстрый ответ, prefill reply из контекстного меню) теперь рендерятся:
  `value={composer.draft}` как plain-const компилировался в одноразовый атрибут и не обновлялся.

## 2026-06-30

### Версия 0.1.26

### Добавлено

* **`Lightbox` — переиспользуемый просмотрщик изображений (обёртка над `viewerjs`).**
  Декларативный Svelte 5 компонент: zoom / pan / rotate / клавиатурная навигация — из коробки
  от `viewerjs`, который полностью скрыт как деталь реализации. API: `images` (`{ src, alt? }[]`),
  `bind:open`, `bind:index`, `onClose`, плюс escape-hatch `options` (мёржится в `new Viewer`).
  - Живёт за отдельным сабпасом **`apartx-ui/lightbox`**; `viewerjs` добавлен в `dependencies`
    и **не** реэкспортится из корневого барреля — потребители без лайтбокса не тянут зависимость
    (как `swiper` за `apartx-ui/carousel`).
  - Ленивая (SSR-safe) загрузка `viewerjs` в `onMount`; `viewer.css` импортится внутри
    компонента — самодостаточно. z-index учитывает слой оверлеев (`getOverlayLayer()`):
    всегда поверх диалога, из которого открыт.
  - Рендерит только скрытую галерею; потребитель держит свои превью и по клику ставит
    `index` + `open = true`. Демо: роут `/lightbox`.
  **Использование:** `import { Lightbox } from 'apartx-ui/lightbox';`

## 2026-06-29

### Версия 0.1.25

### Изменено

* **Убраны последние `<style>`-блоки из компонентов — keyframes переехали в общий CSS.**
  Продолжение 0.1.24: теперь ни один kit-`.svelte` не содержит `<style>` (нет per-component
  scoped-CSS запроса в проде).
  - **`PageLayer`** (движок page-transition: структура `.pt-layer`/`.pt-content` + keyframes
    crossfade / iOS-push / Material shared-axis) → новый `styles/page-transitions.css` (plain
    global; классы частично навешивает JS, поэтому и были `:global`). Поведение идентично.
  - **`Progress`** → `@keyframes indeterminate` (scoped, generic-имя) → глобальный
    `kitIndeterminate` в `styles/animations.css`; разметка `animate-[kitIndeterminate_…]`.
    Побочно чинит латентный рассинхрон: scoped-keyframe переименовывался Svelte'ом, а TW-утилита
    ссылалась на голое имя.
  - Оба файла подключены в баррель `styles/index.css`.
  **Миграция:** потребители, импортящие kit-стили поштучно (не баррель), добавляют
  `@import 'apartx-ui/styles/page-transitions.css';` (как и `animations.css` в 0.1.24).

## 2026-06-29

### Версия 0.1.24

### Изменено

* **`<style>`-блоки убраны из `Header`/`Footer`/`Toolbar` — safe-area теперь на Tailwind.**
  Per-component scoped-CSS давал лишний CSS-запрос в проде. Инсеты переписаны на TW
  arbitrary-классы (едут в общий app.css потребителя):
  - `Header`: `pt-[var(--safe-area-inset-top,0px)] [--kit-safe-top:0px]`;
  - `Footer`: `pb-[var(--safe-area-inset-bottom,0px)] [--kit-safe-top:0px]`;
  - `Toolbar`: `pt-[var(--kit-safe-top,var(--safe-area-inset-top,0px))]`; неиспользуемый
    класс-хук `kit-toolbar` убран.
  Каскад `--kit-safe-top` сохранён (custom-property ставится на сам элемент → наследуется в поддерево).

* **Keyframe-анимации overlay'ев вынесены из `<style>` в общий `styles/animations.css`.**
  `Dialog` (scrim + pop/sheet) и `Drawer` (scrim + slide) не выразить чистым TW (`@keyframes`,
  iOS-safe `rgb()`-scrim, `animation-fill-mode: backwards`), поэтому их CSS переехал из
  per-component `<style>` в новый plain-global файл — едет в едином app.css, без отдельного
  scoped-запроса на overlay. Подключён в баррель `styles/index.css`.
  **Миграция:** потребители, импортящие kit-стили поштучно (а не баррель `apartx-ui/styles`),
  должны добавить `@import 'apartx-ui/styles/animations.css';`.

## 2026-06-29

### Версия 0.1.23

### Изменено

* **Safe-area инсет переехал из `<Dialog>` в примитивы `Toolbar`/`Header`/`Footer`.**
  Откат хардкода v0.1.22 (padding на full-screen-панели `<Dialog>`): знание о safe-area
  теперь живёт в примитиве, который `Dialog` и так композирует, поэтому один механизм
  работает и в диалоге, и на обычной странице.
  - **`Toolbar`** владеет верхним инсетом: `padding-top: var(--kit-safe-top,
    var(--safe-area-inset-top, 0px))` (scoped `:global(.kit-toolbar)`, т.к. класс вешается
    через `cn()`). Покрывает доминирующий паттерн — голый `<Toolbar>` в полноэкранных модалках.
  - **`Header`** резервирует верхний инсет и зануляет `--kit-safe-top` для поддерева, чтобы
    вложенный `<Toolbar>` не дублировал отступ (двух-переменная модель).
  - **`Footer`** резервирует нижний инсет (home-indicator) и тоже зануляет `--kit-safe-top`.
  - **`Dialog`**: full-screen-панель больше не хардкодит padding; центрированная
    (non-fullScreen) зануляет `--safe-area-inset-top/bottom` для поддерева, чтобы
    `Header`/`Toolbar` внутри карточки не давали щель.
  На web/desktop переменные не заданы → 0, поведение не меняется. Затрагивает всех
  потребителей; admin/spaces безопасны (инсет-переменные не выставлены → 0).

## 2026-06-29

### Версия 0.1.22

### Исправлено

* **Полноэкранный `<Dialog>` не учитывал safe-area (статус-бар / home-indicator).**
  Контент `fullScreen`-диалога рендерится `absolute inset-0` внутри портала (вне `#app`
  потребителя), поэтому app-овый `padding-top` на него не действовал — `Header`/`Toolbar`
  модалки оказывались под статус-баром в Cordova. На full-screen-панель добавлены
  `pt-[var(--safe-area-inset-top,0px)]` + `pb-[var(--safe-area-inset-bottom,0px)]` (паттерн
  как у `BottomNav`, который уже уважал нижний inset). На web/desktop переменные не заданы
  → 0, поведение не меняется. Затрагивает всех потребителей (admin/cabinet/spaces) —
  безопасно: где inset-переменные не выставлены, padding схлопывается в 0.

## 2026-06-29

### Версия 0.1.21

### Добавлено

* **History-back для модалок модал-реестра.** `<Dialog>`, отрендеренный реестром (внутри
  `<ModalLayer>` → присутствует overlay-layer контекст), теперь зеркалит своё `open` в
  overlay-stack: открытие кладёт синтетическую same-URL history-запись, а браузерный/
  нативный BACK закрывает ВЕРХНюю модалку — флипая `open=false` (тот же путь, что крестик/
  Esc), поэтому **проигрывается обычная exit-анимация**, а страница не навигируется.
  Закрытие любым способом (крестик/Esc/кнопка-назад/программно) снимает запись; идемпотентно
  — back и не-back закрытие не дёргают history дважды. Интеграция **гейтится** по
  `getOverlayLayer()`: standalone-`Dialog` (без реестра, напр. в spaces/admin) не затронуты.
  Единственный back-interceptor вешает `<ModalOutlet>` один раз на клиенте
  (`initOverlayStack`). Раньше модалки реестра вообще не реагировали на BACK (уходили в
  роутер страниц).

## 2026-06-28

### Версия 0.1.20

### Исправлено

* **Модал-реестр ремаунтил ВСЕ открытые модалки при любом изменении стека.**
  `<ModalOutlet>` ждал `registry[id].load()` инлайн в `{#each}` `{#await}`; открытие/
  закрытие вложенной модалки переподнимало лоадер (новый промис) и **ремаунтило** ветку
  `then` — молча сбрасывая локальный `$state` родительской модалки на полпути (напр.,
  `CartModal` терял выбранные даты в момент открытия пикера поверх него). Так как Svelte 5
  ремаунтит `then` даже при стабильном промисе на ре-рендере хоста, лечение двойное:
  (1) мемоизация промиса чанка по id в реестре (`loadModal`), (2) новый `ModalHost`
  (по одному на `m.key`) резолвит компонент **один раз** в `$state` и рендерит
  `{#if Comp}<Comp/>` (без инлайн-`{#await}`). Стабильная ссылка `Comp` ⇒ Svelte больше
  не ремаунтит модалку при изменениях соседних слоёв стека.

* **`DateRangePicker` не пробрасывал выбранный диапазон.** Сегменты заполнялись, но
  двунаправленный `$effect` между внутренним `DateRange` и связанной строкой `{start,end}`
  затирал себя — выбор в календаре не доходил до потребителя. Перевели на `onValueChange`
  bits-ui как единственный источник истины (value→range остался охранённым эффектом).

* **Календари date-пикеров открывались ЗА слоёным `Dialog`** (z-index) и **обрезались**.
  Контент `bits-ui` рендерился на `z-50` (тот же слой, что и `Dialog` реестра) — клики по
  дням/Next перехватывались. Чиним как у `Combobox`: читаем `getOverlayLayer()` и рендерим
  контент на `layer.z + 2`. Плюс `DateRangePicker` (в отличие от `DatePicker`) **не** был
  обёрнут в `<…Portal>` — попап обрезался `overflow` диалога; добавили `Portal`.

## 2026-06-27

### Версия 0.1.19

### Исправлено

* **`Combobox`: попап открывался ЗА слоёным `Dialog` модал-реестра.** Дропдаун
  портируется в `<body>` с `z-50`, но `Dialog` (обёрнутый в `<ModalLayer>`) рендерит
  контент на `layer.z + 1` (≈61) — любой комбобокс внутри слоёного диалога уходил под
  него и опции были некликабельны. Теперь компонент читает тот же `getOverlayLayer()`, что
  и `Dialog`, и рендерит портал-контент на `layer.z + 2` (над своим диалогом, под более
  глубокой модалкой); без слоя — прежний `z-50` (обратно совместимо, как `Dialog`/
  `BottomSheet`).
* **`Combobox`: значение не отображалось, если приходило ПОСЛЕ маунта** (асинхронный
  fetch опций, сидинг формы редактирования). Корень — `defaultValue` у `Combobox.Input`
  (bits-ui) не реактивный (применяется один раз при маунте), поэтому поздно пришедший
  `value`/`options` навсегда оставлял инпут на плейсхолдере. Перешли на реактивный бокс
  Root `inputValue`: пока попап открыт — показываем поисковый запрос, когда закрыт —
  label выбранной опции (реактивно; disabled-комбобокс тоже показывает значение).

### Версия 0.1.18

### Добавлено

* **`PageTransition`: проп `slideStyle` (`'auto' | 'ios' | 'shared-axis'`).** Осознанный
  выбор стиля анимации смены страниц вместо неявного дефолта. `'ios'` — горизонтальный
  push-слайд в стиле iOS (входящий слой выезжает поверх, уходящий слегка уезжает под
  него); `'shared-axis'` — Material shared-axis (слой целиком уезжает по оси с
  затуханием). `'auto'` (дефолт) резолвится по ОС через `detectMobileOS`: iOS → `'ios'`,
  остальное → `'shared-axis'`. Стиль прокидывается в DOM как `data-slide-style`, а
  keyframes заскоуплены по этому атрибуту — оба стиля работают рядом без протечек.
* **`detectMobileOS` (`apartx-ui/utils`)** — лёгкий детект мобильной ОС (iOS/Android/other)
  по UA, для резолва `slideStyle='auto'`. Покрыт первым Vitest-харнессом кита.
* **Экспорт типа `SlideStyle`** из `apartx-ui/navigation` для типобезопасного
  использования пропа потребителями.

### Исправлено

* **`shared-axis`: bleed/flash-free.** Затухание применяется ко всему слою (а не только
  к контенту) и на forward-in, и на back-exit — нет вспышки фона и «протечки» соседнего
  слоя на стыке транзишена.

## 2026-06-16

### Версия 0.1.17

### Изменено

* **`Image`: сессионный кэш загруженных картинок (`<script module>`).** Модульный
  `Set<string>` хранит `src`, которые хотя бы раз успешно загрузились. При повторном
  монтировании `<Image>` с уже известным `src` стартует сразу в статусе `loaded`
  (без skeleton, `opacity-100` с первого кадра) вместо повторного прохода
  `loading → loaded`. Раньше каждый ремоунт переигрывал skeleton + 300ms fade, хотя
  байты уже были в кэше браузера — это давало моргание (в списках, при навигации
  туда-обратно). Кэшируются только успешные загрузки, так что ошибки по-прежнему
  идут через повтор. SSR/гидрация не ломаются: на первом клиентском рендере кэш пуст
  (заполняется только после `onload`), выигрыш — на последующих ремоунтах.

## 2026-06-16

### Версия 0.1.16

### Исправлено

* **`RangeCalendar`: локализация названий месяцев и дней недели.** Добавлен опциональный
  проп `locale` (BCP-47, дефолт `'en-US'`), который пробрасывается в `bits-ui`
  `RangeCalendar.Root`. Раньше проп не передавался, и bits-ui (`@internationalized/date`)
  всегда форматировал заголовок месяца и шапку дней недели по дефолтной `'en-US'` —
  календарь оставался английским при любом языке приложения. Дефолт сохраняет прежнее
  поведение, так что для `apartx-admin`/`apartx-cabinet` изменение не ломающее.

## 2026-06-16

### Версия 0.1.15

### Добавлено

* **`Image` — новый компонент (`apartx-ui/display`).** Картинка со skeleton-плейсхолдером
  на время загрузки и состоянием ошибки с кнопкой повтора. Защита от layout-shift через
  bits-ui `AspectRatio` (padding-% хак, безопасно для legacy-браузеров) при заданном `ratio`,
  иначе размер берётся из класса. Пропсы по конвенциям кита: `src`, `alt`, `ratio`, `fit`,
  `loading`, `errorText`, `retryText`, `showError`, `onStatusChange(status)`, `onRetry`,
  `class`, `imgClass`. Кнопка повтора `stopPropagation`'ит, чтобы тап не «проваливался» в
  навигацию родительской карточки.

### Исправлено

* **`Carousel`: пропсы `navigation` / `pagination` / `loop` / `autoplay` передаются булевыми,
  а не строками `'true'`/`'false'`.** `swiper-container` объявляет эти параметры как JS-свойства,
  чей сеттер кладёт значение как есть (без `formatValue`), а Svelte 5 для односложных пропов
  пишет именно свойство. Строка `'false'` — truthy → Swiper рисовал стрелки `<>` навигации и
  включал loop/autoplay, даже когда они выключены. Булевы значения сохраняются как настоящие
  `false`/`true`. (`css-mode` не затронут: его дефисное имя идёт через атрибут, где `'true'`/
  `'false'` парсятся корректно.)
* **`BottomSheet`: горизонтальный свайп вложенного контента (карусель) работает и в нижней
  позиции шторки.** Добавлен axis-lock: на первых ~6px жеста он фиксируется как `'x'`
  (горизонтальный — отдаётся дочернему скроллеру, шторку не тащит и не `preventDefault`'ит)
  или `'y'` (вертикальный — обычный перехват шторки/списка). Раньше ниже верхнего снапа
  `shouldDrag()` перехватывал ЛЮБОЙ тач как драг шторки и `preventDefault`'ил нативный
  горизонтальный скролл карусели (в верхнем снапе работало, т.к. там ветка по направлению
  возвращала false). Вертикальное поведение драга/списка не изменилось.

## 2026-06-15

### Версия 0.1.14

### Добавлено

* **`PageTransition` / `PageLayer`: проп `providePortalHost` (default `true`).** Вложенный
  `<PageTransition>` (контент таба внутри shell) можно пометить `providePortalHost={false}`,
  чтобы `<BottomSheet portalTarget="page">` портировался во ВНЕШНИЙ, полноэкранный слой,
  который реально слайдится при push'е страницы, а не во внутренний (ограниченный футером)
  слой — иначе шторка не «едет» вместе с переходом. admin/cabinet не затронуты (дефолт
  `true` = прежнее поведение).
* **`BottomSheet`: проп `animate` (default `true`).** При `false` present/dismiss мгновенные
  (без слайда и фейда бэкдропа) — чтобы восстановить уже открытую шторку (возврат на карту)
  без повторной анимации; затем хост снова включает анимацию.
* **Google Maps: кластеризация.** Провайдер google лениво подгружает
  `@googlemaps/markerclusterer` (UMD c jsDelivr, как и yandex-кластерер). `ClusterPoint`
  получил `renderKey` — токен внешнего вида: при его смене для существующего `id` маркер
  пере-стилизуется на месте (без смены `id` → набор точек стабилен, слой не
  пере-кластеризуется и не моргает).

### Изменено

* **Mobile-переход — НЕПРОЗРАЧНЫЙ слайд (iOS-style).** Ведущая страница едет на всю ширину
  (`translateX(100%)` → `translateZ(0)`) и полностью кроет уходящую. Settled-transform =
  `translateZ(0)`, а НЕ `none`: слой с `transform:none` перестаёт быть containing block для
  `position:fixed`-потомков, и портированная шторка на один кадр ре-рутится между
  композитными слоями — сквозь неё мелькает фон (напр. нижняя навигация). Раньше был
  30%-слайд + opacity-fade, через полупрозрачность которого просвечивала уходящая страница.
* **`MapClusterer` (fallback-маркеры): реконсиляция по `id`.** Маркеры хранятся в `Map` по
  `id`; при апдейте создаются/удаляются только изменившиеся, а не пересоздаётся весь набор —
  нет мерцания всех пинов при ре-derive `points` (переключение выделения, новые результаты).

---

## 2026-06-11

### Версия 0.1.13

### Изменено

* **`PageTransition.direction` принимает `() => Direction`** (обратносовместимо — строка
  работает ровно как раньше, admin/cabinet не затронуты). `direction` теперь резолвится
  **лениво** (`resolveDir()`) на enter и outro. Зачем: когда хост, владеющий направлением,
  сносится не своим `{#key}`, а **ANCESTOR-блоком** (вложенный роутер-shell уходит на
  детальный роут), value-prop замораживается на последнем рендере хоста, и уходящий слой
  читает **устаревшее** направление (Svelte `derived_inert`). Функция читает живое
  состояние на outro. `enterKind` получил `key` в зависимости, чтобы функция
  пере-резолвилась на каждую навигацию (ссылка на функцию стабильна). См. `SlotLayout` в
  apartx-spaces.

---

## 2026-06-10

### Версия 0.1.12

### Добавлено

* **`BottomSheet portalTarget="page"` — оверлей едет вместе со страницей при переходе.**
  По умолчанию (`'body'`) шит порталится в `<body>` (как раньше — admin/cabinet не
  затронуты). При `'page'` порталится в элемент текущего слоя `PageTransition` (его
  отдаёт `PageLayer` через новый контекст `set/getPagePortalHost` в `navigation/context`),
  поэтому при навигации шит уезжает/приезжает СО страницей, а не висит на месте. Нет
  `PageTransition`-предка → fallback на `<body>`.

### Изменено

* **`PageLayer` приведён к контракту «скроллит только `<Content>`».** Слой `.pt-layer`
  стал чистой сценой трансформа (`overflow: visible`, НЕ скролл-контейнер); скролл
  перенесён на внутренний `.pt-content` (`height:100%; overflow-y:auto`), туда же —
  `use:scrollRestore`. Root `PageTransition` сменил `overflow-hidden` → `overflow-clip`
  (клипует слайд, но НЕ скроллируемый). Это устраняет первопричину рывка портального
  `position:fixed`-оверлея: фокус Dialog'а при открытии делал scroll-into-view и дёргал
  скроллируемый трансформ-предок; теперь в цепочке предков скроллера нет вовсе. Страницы
  со своим `<Content>` (admin/cabinet) не меняют поведение — `<Content>` как и прежде
  владеет скроллом, внешний `.pt-content` дормантен.

### Версия 0.1.11

### Добавлено

* **CSS-barrel `apartx-ui/styles`** — один future-proof импорт вместо пофайловых.
  Новый `src/lib/styles/index.css` агрегирует `tokens.css` + `typescale.css` +
  `utilities.css` (Tailwind v4 разворачивает вложенный `@import`-граф в контексте
  потребителя, так что `@theme`/`@utility` обрабатываются как раньше). В `exports`
  добавлен ключ `"./styles"` → `index.css`. Потребитель пишет одну строку
  `@import 'apartx-ui/styles';` после `@import 'tailwindcss';`; будущие kit-стили
  добавляются в barrel и подтягиваются без правок entry у потребителей. Пофайловый
  wildcard `"./styles/*"` сохранён для обратной совместимости (admin/cabinet всё ещё
  импортят `tokens.css`/`typescale.css` напрямую — мигрируют на barrel по мере удобства).

### Версия 0.1.10

### Добавлено

* **`no-scrollbar` (`apartx-ui/styles/utilities.css`)** — Tailwind v4 `@utility`,
  прячущая нативный скроллбар (`scrollbar-width:none` + `-ms-overflow-style` +
  `::-webkit-scrollbar{display:none}`); контент всё равно скроллится. Потребители
  импортируют файл рядом с `tokens.css`/`typescale.css`
  (`@import 'apartx-ui/styles/utilities.css';`).

### Изменено

* **`Content` и `VirtualList` — скроллбар скрыт до первого скролла.** Браузер «мигает»
  дефолтным скроллбаром при любом изменении размера контента скроллера (первый рендер,
  асинхронный/клиентский контент), и он висит до первого взаимодействия — визуально шумно.
  Теперь полоса скрыта утилитой `no-scrollbar` до первого **пользовательского** скролла,
  после чего нативное поведение возвращается. В `VirtualList` программный restore-скролл
  (флаг `restoring`) не считается «первым скроллом». Поведение глобальное для всех
  потребителей kit (admin/cabinet/spaces — каждый импортит `utilities.css`).

### Версия 0.1.9

### Добавлено

* **`RangeCalendar` (`apartx-ui/forms`)** — всегда-видимый (inline) range-календарь
  на bits-ui `RangeCalendar`, с той же разметкой/стилями ячеек, что и поповер-календарь
  `DateRangePicker`, но без поля/триггера. Для модалок/шторок, где календарь показывается
  напрямую. **Растягивается на всю доступную ширину** (ячейки `flex-1`), полоса выбранного
  диапазона тянется на всю строку. Пропы: `value` (bindable `{start,end}`, строки
  YYYY-MM-DD), `minValue`, `maxValue`, `numberOfMonths` (по умолчанию 1), `class`.

### Версия 0.1.8

`BottomSheet` проверен на устройстве; `CupertinoPane` удалён.

### Удалено

* **`CupertinoPane` удалён (заменён `BottomSheet`).** Компонент и его barrel-экспорт
  (`apartx-ui/overlays`) убраны: после миграции search-шторки на `BottomSheet` ни один
  потребитель кита (spaces/admin/cabinet) его не импортировал. Опциональная зависимость
  `cupertino-pane` больше не нужна — консьюмеры, у которых она стояла только ради этого
  компонента, могут её убрать (в apartx-spaces убрана). Lifecycle-события `BottomSheet`
  (`onWillDismiss`/`onDidDismiss`) повторяют прежний API `CupertinoPane`, так что
  семантика для хоста сохранилась.

## 2026-06-09

### Версия 0.1.7

Новый `BottomSheet` — bottom-sheet со снап-поинтами на bits-ui `Dialog`.

### Добавлено

* **`BottomSheet` — draggable bottom-sheet на bits-ui `Dialog`.** Оболочка — `Dialog`
  (`Portal` в `<body>`, бэкдроп, focus-trap, escape, scroll-lock), консистентно с
  `Dialog`/`AlertDialog`. Слой жеста (snap-поинты как доли `0..1`/px, drag, физика
  релиза по velocity+ближайшему снапу, handoff «тянем шторку ↔ скроллим контент» по
  позиции скролла) — свой, на рунах, адаптирован из vaul-svelte (MIT, huntabyte).
  `Portal` в `<body>` снимает корневую боль cupertino-pane: пейн больше не привязан к
  родителю/хедеру и не зависит от внутренних замеров библиотеки. API: `open` (bindable),
  `snapPoints`, `activeSnapPoint` (bindable), `dismissible`, `backdrop`/`backdropOpacity`,
  `squareCornersAtTop`, `showHandle`, `modal`, `onSnapChange`/`onOpenChange` +
  lifecycle `onWillDismiss` (slide-out start, content still shown) / `onDidDismiss`
  (fully closed & unmounted; cancelled if reopened first) — те же имена, что у
  `CupertinoPane`. Появление/уход анимированы (выезд снизу + затухание бэкдропа).
  `CupertinoPane` пока остаётся; миграция search-шторки — первый потребитель.

### Версия 0.1.6

`CupertinoPane`: не шлёт dismiss-события при размонтировании; не презентует дважды на маунте. `MapView`: хром карты изолирован под оверлеями.

### Исправлено

* **`MapView` — изолирующий stacking context (`isolate`).** Хром провайдера (у Yandex —
  копирайт / ссылка «Открыть Яндекс Карты») пинится с очень высоким z-index и без
  изоляции конкурировал в корневом stacking-контексте → рендерился ПОВЕРХ оверлеев
  приложения (например, шторки `CupertinoPane` на z-50). Корневой `<div>` карты получил
  `isolation: isolate`, поэтому весь хром карты теперь живёт под любым внешним оверлеем
  независимо от внутренних z-index провайдера. Kit-овые M3-контролы (z-10) продолжают
  работать внутри этого контекста.

* **`CupertinoPane` — двойной present на маунте убран.** `createPane()` презентовал
  пейн, и сразу следом show-`$effect` презентовал его ВТОРОЙ раз. Второй вызов
  cupertino `present()` на уже отрендеренном пейне делает `moveToBreak(initialBreak)` —
  то есть затирал любой программный брейк, выставленный хостом из `onDidPresent`
  (например, restore последнего брейка при «назад»). Введён флаг `lastShow` (последнее
  применённое значение show): show-`$effect` презентует/прячет только при РЕАЛЬНОЙ смене,
  а маунтовый прогон, где createPane уже презентовал, пропускается. Для консьюмеров без
  программного moveToBreak поведение не меняется (тот же конечный брейк).

* **`CupertinoPane` — onWillDismiss/onDidDismiss больше не дёргаются при unmount.**
  cupertino `destroy()` эмитит оба dismiss-события (как при свайпе/тапе по бэкдропу), а
  kit зовёт `pane.destroy()` в onMount-cleanup — поэтому при размонтировании компонента
  (например, SPA-переход на другую страницу) хост получал ложный «dismiss». Если хост на
  onDidDismiss чистит сохранённое состояние шторки (для restore при «назад»), оно стиралось
  ровно в момент ухода со страницы. Введён флаг `destroyingOnUnmount` (ставится в
  onMount-cleanup ДО `destroy()`): обёртки onWillDismiss/onDidDismiss при teardown'е
  ни хоста не уведомляют, ни `createPane()` не зовут (re-arm воскрешал бы уходящую шторку).

## 2026-06-08

### Версия 0.1.5

Bottom-sheet поверх карты: тёма/z-index/острые углы `CupertinoPane`, хуки камеры карты и брейка шторки.

### Добавлено

* **`MapView` — `onCameraChange({ center, zoom })`.** Дёргается в конце жеста пана/зума (Yandex: `YMapListener.onActionEnd`, Google: `idle`). Для персиста позиции карты (например, в URL). `setCenter`-эффект `MapView` получил epsilon-guard, чтобы запись камеры обратно в пропы не пере-применяла ту же позицию и не зацикливалась.
* **`CupertinoPane` — `onBreakChange(break)`.** Отдаёт брейк (`top`/`middle`/`bottom`), вычисленный по реальной позиции (`getPanelTransformY()` против `breakpoints.breaks`), а не через библиотечный `currentBreak()`, который рассинхронизируется. Дёргается на drag/transition-settle.
* **`CupertinoPane` — `squareCornersAtTop`.** На верхнем брейке верхние углы шторки становятся острыми (0), на остальных — скруглённые. Завязано на позицию (`getPanelTransformY`), не на ненадёжный `currentBreak()`; обновляется на `onDrag`/`onDragEnd`/`onTransitionEnd`/`onDidPresent` + trailing-таймер (библиотечный `transitionend` на drag-snap ненадёжен — браузерный баг). Проброшен `onDrag` как pass-through проп.

### Изменено

* **`CupertinoPane` — тема и z-index по токенам кита.** Фон/текст/хэндл/destroy-кнопка маппятся на M3-токены (`--color-surface-container`/`-on-surface`/…), шторка следует светлой/тёмной теме приложения вместо хардкод-белого. `.pane`/`.backdrop` подняты в overlay-диапазон кита (z-50/z-40), чтобы шторка была над page-chrome (FAB z-20, контролы карты z-10).

## 2026-06-08

### Версия 0.1.4

Провайдер-независимые контролы карты (kit-rendered M3), escape-hatch для нативных опций, фиксы темы хрома и загрузки кластеризатора.

### Добавлено

* **`MapView` — провайдер-независимые контролы.** Проп `controls: boolean | { attribution, zoom, geolocation, scale, layer }` (общий резолвер `providers/controls.ts`). `attribution`/`scale` — нативные пассивные (Yandex `copyrights`/`YMapScaleControl`, Google `disableDefaultUI`/`scaleControl`); `zoom`/`geolocation`/`layer` — **kit-rendered M3-кнопки** (`maps/controls/`), которые дёргают карту через новый императивный API `MapHandle` (`zoomIn`/`zoomOut`, `getLayer`/`setLayer`/`availableLayers`). Geolocation — на стороне кита через `navigator.geolocation` → `setCenter`, нативный контрол не нужен. `availableLayers()` отдаёт только поддержанные провайдером типы (Yandex map/satellite, Google map/satellite/hybrid) — свитчер не предлагает врущих тогглов. Inline-SVG в кнопках (кит по дизайну не тянет icon-сеты).
* **`MapView` — `controlsPosition`** (`top-left|top-right|bottom-left|bottom-right`, дефолт `bottom-right`) для оверлея контролов.
* **`MapViewOptions.providerOptions`** — escape-hatch для нативных опций провайдера без кросс-провайдерного аналога, по ключу провайдера (`{ yandex: {…}, google: {…} }`); применяется подобъект активного провайдера, мержится в нативную карту последним (перекрывает нормализованные пропы).

### Изменено

* **`MapProviderName`** — единый источник в `providers/types.ts` (раньше дублировался в `providers/index`), ре-экспорт сохранён.
* **Google-провайдер** — симметрия с Yandex: нормализованные `controls`/`scale`/`layer` (`mapType`)/`zoom`.
* **`MapConfig`** — обновляет поля `config` гранулярно, а не заменяет объект целиком: смена темы больше не инвалидирует читателей `apiKey`/`lang`/`mapId`, поэтому `MapView` ретинтит карту на месте, а не пересоздаёт её (пересоздание переподключало кластеризатор и роняло маркеры на каждый тоггл темы).

### Исправлено

* **Тема хрома `YMap`.** Тема задавалась только слою тайлов (`YMapDefaultSchemeLayer`), а копирайт/distribution/контролы остаются на теме самого `YMap` — в dark-mode хром оставался светлым (белый фон кнопки) и наследовал белый текст приложения → «Открыть Яндекс Карты» белым по белому. Тема теперь прокидывается и в `YMap` (и обновляется в `setTheme`).
* **Загрузка кластеризатора Yandex.** `ymaps3.import` без зарегистрированных loader'ов падал с `no loader for pkg @yandex/ymaps3-clusterer` (молчаливый деград на plain-маркеры). Перед импортом регистрируется CDN (`ymaps3.import.registerCdn`) и используется версионированный пакет `@yandex/ymaps3-clusterer@0.0.1`.

## 2026-06-07

### Версия 0.1.3

`VirtualList` — восстановление позиции скролла по `name`; пресет `ChatList` переименован в `MessagesList`.

### Добавлено

* **`VirtualList` — restore скролла через проп `name`.** По аналогии с `<Content name=…>`: задан `name` → позиция запоминается/восстанавливается между ремаунтами (например, навигация туда-обратно), `name` служит ключом в module-scoped `Map`. Для виртуального списка наивный `scrollTop` неточен (в DOM только видимые строки), поэтому сохраняем virtua `CacheSnapshot` (измеренные высоты) + offset: высоты восстанавливаем через `cache`-проп `VList` (блок `{#key name}` пересоздаёт список с нужным кэшем), offset — через `scrollTo` после маунта с retry до 12 кадров (virtua подключает скроллер на кадр позже и клампит, пока высоты не устаканятся) и подавлением сохранений на время restore. Без `name` поведение прежнее. Экспортирован `clearVirtualScroll(name)`.

### Изменено

* **Пресет `ChatList` → `MessagesList`.** Имя вводило в заблуждение: компонент — пресет для ленты *сообщений* (stick-to-bottom), а не списка чатов. Файл, barrel-экспорт (`apartx-ui/virtual`) и JSDoc обновлены. Консьюмеров `ChatList` не было.

## 2026-06-05

### Версия 0.1.2

`Toolbar` разносит элементы по умолчанию; длинные опции `Select` переносятся.

### Изменено

* **`Toolbar` — отступ и перенос между элементами из коробки.** Контейнер children получил `flex-wrap` + `gap-2`: раньше у него не было `gap`, и любой `gap-*`, переданный на сам `Toolbar` через `class`, на расстояние между детьми не влиял (разносил только `start | children | end`) — из-за чего несколько элементов слипались. Добавлены escape-hatch пропсы `startClass`/`contentClass`/`endClass` для точечного переопределения обёрток слотов (например `contentClass="gap-4"`).
* **`Select` — перенос длинных опций.** Пункты выпадающего списка (`min-w-0 break-words`, выравнивание по верху) переносятся на вторую строку вместо обрезки — длинные значения без пробелов (`lock_renew_subscription_agent_reward`) больше не вылезают за ширину панели.

## 2026-06-03

### Версия 0.1.1

Адаптивная пагинация для мобильных экранов.

### Изменено

* **`Pagination` — responsive-вёрстка.** На узких экранах (`< sm`) компонент раскладывается в 3 центрированные строки: счётчик `1–10 of 308` → «Rows per page» → навигация. Числовой ряд страниц на мобиле компактный (первая … текущая … последняя, новый `pageItemsMobile`), полная нумерация — на `sm+`. Порядок строк управляется `order`-классами, разметка кнопок страниц вынесена в сниппет `pager` (без дублирования mobile/desktop). API не изменился.
