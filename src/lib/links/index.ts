// `apartx-ui/links` — app link registry. See registry.ts for the contract.
export { setLinkRegistry, getLinkRegistry, parseLink, openLink, linkShareUrl } from './registry';
export type { AppLink, LinkHandler, LinkRegistry } from './registry';
